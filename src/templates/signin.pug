extends layout

block title
  | Sign In — test-frontend

block footer-logo
  .footer-logo.footer-logo--noslogan

block content
  .page.page--signup
    .auth
      .auth-head
        .auth-head-title Login
        a.auth-head-button.auth-head-button--signup(href = '#')
          span Create account

      form.form
        .form-input.form-input--focus
          .form-input-label Email
          input.form-input-text.form-input-text--email(type = 'text'
                                                       autocomplete = 'off'
                                                       value = 'francis@gmail.com')

        .form-input
          .form-input-label Password
          input.form-input-text.form-input-text--password(type = 'password'
                                                          autocomplete = 'off')

        button.form-button
          span login
